# FS-template-review-fixture: Template Review Fixture

This tiny specification exists so the spec-review example can be copied and run
without depending on an absolute checkout path.

## 1. Requirements

- The reviewer must check consistency, completeness, correctness, and clarity.
- The fixer must keep changes scoped to this fixture unless the review finding
  explicitly names another file.
