### Task analyze: Analyze {{subject}} and dispatch work
**State:** analyze

Analyze `{{subject}}` against the brief below, then create one follow-up task
per work item you find (up to {{max_tasks}}). Follow the `analyze` state
instructions for the exact task-file format and dependency wiring — new tasks
are added by writing `tasks/NN-<slug>.md` files into this workspace.

**Analysis brief:**

{{analysis_brief}}
