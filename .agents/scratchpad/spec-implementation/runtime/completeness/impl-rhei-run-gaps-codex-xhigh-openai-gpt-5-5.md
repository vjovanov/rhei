# Completeness Audit: `docs/functional-spec/rhei-run.spec.md`

**Auditor:** codex[xhigh]:openai:gpt-5.5
**Spec source:** `runtime/manifests/impl-rhei-run-spec.txt` -> `docs/functional-spec/rhei-run.spec.md`
**Scope:** implementation in the repository, principally `crates/rhei-cli/src/main.rs`, `crates/rhei-tui/src/*.rs`, and `crates/rhei-validator/src/lib.rs`.
**Method:** enumerate normative claims in the assigned spec and classify each as `covered`, `partial`, `missing`, or `not-normative`. Code quality is not assessed.

## 1. Framing And Usage

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 3 | `rhei run` drives a plan end-to-end by repeatedly finding ready work, spawning a state agent or program, waiting, and transitioning. | partial | The command loads the plan and dispatches to run loops at `crates/rhei-cli/src/main.rs:8313` and `crates/rhei-cli/src/main.rs:8357`. Agent and program subprocesses are spawned at `crates/rhei-cli/src/main.rs:9048` and `crates/rhei-cli/src/main.rs:8711`, and transitions are applied at `crates/rhei-cli/src/main.rs:10309`. Gaps below cover readiness, polling, program parallelism, snapshot effects, and final exit semantics. |
| 3 | `rhei run` operates under orchestrator authority: the orchestrator, not the spawned subprocess, owns every state transition. | covered | The agent prompt tells spawned agents not to call `rhei transition` or `rhei complete` at `crates/rhei-cli/src/main.rs:7300`. The run loop applies transitions itself through `try_auto_advance_task` / `execute_transition` at `crates/rhei-cli/src/main.rs:10309`. |
| 5 | Live terminal UI behavior is specified separately. | not-normative | Cross-reference only. |
| 10 | CLI form is `rhei run <RHEI_PLAN_OR_WORKSPACE> [flags]`. | covered | `Commands::Run` defines positional `input: PathBuf` and flattened flag groups at `crates/rhei-cli/src/main.rs:216`. Dispatch passes that input to `run_command` at `crates/rhei-cli/src/main.rs:553`. |

## 2. Options

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 15 | Flags are grouped by concern. | covered | Clap help headings are defined for standalone, agent, program, and snapshot groups at `crates/rhei-cli/src/main.rs:5542`, `crates/rhei-cli/src/main.rs:5572`, `crates/rhei-cli/src/main.rs:5590`, and `crates/rhei-cli/src/main.rs:5605`. |
| 21 | `--dry-run` defaults false and prints planned transitions without executing them. | partial | The flag exists at `crates/rhei-cli/src/main.rs:5544` and defaults false in `default_run_options` at `crates/rhei-cli/src/main.rs:7014`. Dry-run branches skip subprocesses at `crates/rhei-cli/src/main.rs:8650` and `crates/rhei-cli/src/main.rs:8914`, but output format and artifact behavior diverge from the Dry Run section. |
| 22 | `--no-callbacks` defaults false and skips `on_leave` / `on_enter` callbacks. | covered | Flag and default at `crates/rhei-cli/src/main.rs:5547` and `crates/rhei-cli/src/main.rs:7015`; callbacks are skipped when `no_callbacks` is true at `crates/rhei-cli/src/main.rs:5295` and `crates/rhei-cli/src/main.rs:5473`. |
| 23 | `--continue-on-error` defaults false and continues after agent or program non-zero exit. | covered | Flag/default at `crates/rhei-cli/src/main.rs:5550` and `crates/rhei-cli/src/main.rs:7016`; non-zero agent/program branches return only when it is unset at `crates/rhei-cli/src/main.rs:8837`, `crates/rhei-cli/src/main.rs:9199`, and `crates/rhei-cli/src/main.rs:9488`. |
| 24 | `--parallel <N>` defaults 1, limits concurrent agents or programs, and `0` means unlimited. | partial | Flag/default at `crates/rhei-cli/src/main.rs:5553` and `crates/rhei-cli/src/main.rs:7017`; agent `0 = unlimited` batching is implemented at `crates/rhei-cli/src/main.rs:8910`. Program states are still executed sequentially in the loop at `crates/rhei-cli/src/main.rs:8672`, so the "agents or programs" concurrency claim is not fully covered. |
| 25 | `--tui` forces TUI mode; default is auto. | covered | Flag at `crates/rhei-cli/src/main.rs:5556`; `RunOptions::frontend_kind` returns TUI when set and Auto otherwise at `crates/rhei-cli/src/main.rs:5648`. |
| 26 | `--no-tui` forces plain stdout; default is auto. | covered | Flag at `crates/rhei-cli/src/main.rs:5559`; `RunOptions::frontend_kind` returns stdout when set and Auto otherwise at `crates/rhei-cli/src/main.rs:5651`. |
| 32 | `--no-agent` disables agent spawning and uses callback-only advancement. | covered | Flag at `crates/rhei-cli/src/main.rs:5574`; `resolve_agent_invocations` returns no invocations when set at `crates/rhei-cli/src/main.rs:6803`, and such tasks are put into `callback_tasks` at `crates/rhei-cli/src/main.rs:8507`. |
| 33 | `--agent <AGENT>` overrides the agent for this run. | covered | Flag at `crates/rhei-cli/src/main.rs:5577`; resolution gives CLI override precedence at `crates/rhei-cli/src/main.rs:6701`. |
| 34 | `--agent-mode <MODE>` overrides the agent mode. | covered | Flag at `crates/rhei-cli/src/main.rs:5580`; resolution gives CLI override precedence at `crates/rhei-cli/src/main.rs:6731`. |
| 35 | `--model <MODEL>` overrides the model. | covered | Flag at `crates/rhei-cli/src/main.rs:5583`; resolution gives CLI override precedence at `crates/rhei-cli/src/main.rs:6687`. |
| 41 | `--from-snapshot <ref>` overrides the concrete source selected by authored `snapshot.inherit:` after constraints are applied. | partial | The flag and accessor exist at `crates/rhei-cli/src/main.rs:5607` and `crates/rhei-cli/src/main.rs:5692`, and the preload hook receives it at `crates/rhei-cli/src/main.rs:10380`. The hook is a no-op at `crates/rhei-cli/src/main.rs:10375`, so no runtime override occurs. |
| 42 | `--override-inherit` bypasses authored source-selection and compatibility constraints; target state must still declare `snapshot.inherit:`; requires `--from-snapshot`. | partial | Clap enforces `requires = "from_snapshot"` at `crates/rhei-cli/src/main.rs:5613`, and the accessor is passed to the preload hook at `crates/rhei-cli/src/main.rs:10381`. The runtime bypass and "target state must still declare `snapshot.inherit:`" guard are missing because the preload hook discards inputs and returns `Ok(())` at `crates/rhei-cli/src/main.rs:10375`. |
| 43 | `--task <id>` selects the task for an ambiguous snapshot override. | partial | Flag/accessor exist at `crates/rhei-cli/src/main.rs:5615` and `crates/rhei-cli/src/main.rs:5700`, and are passed to the no-op preload hook at `crates/rhei-cli/src/main.rs:10382`. No ambiguity resolution is implemented. |
| 44 | `--target <slug>` selects the fanout target for an ambiguous snapshot override. | partial | Flag/accessor exist at `crates/rhei-cli/src/main.rs:5618` and `crates/rhei-cli/src/main.rs:5704`, and are passed to the no-op preload hook at `crates/rhei-cli/src/main.rs:10383`. No ambiguity resolution is implemented. |
| 50 | `--no-program` disables program spawning and uses callback-only advancement for program states. | covered | Flag at `crates/rhei-cli/src/main.rs:5592`; `resolve_program` returns none when set at `crates/rhei-cli/src/main.rs:7214`, and program states are added to `callback_tasks` at `crates/rhei-cli/src/main.rs:8495`. |
| 51 | `--program-timeout <DURATION>` overrides program timeout per program state. | covered | Flag/accessor at `crates/rhei-cli/src/main.rs:5595` and `crates/rhei-cli/src/main.rs:5688`; `resolve_program` applies it before state/settings defaults at `crates/rhei-cli/src/main.rs:7226`. |

## 3. Execution Loop

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 55 | `rhei run` runs passes until no further forward progress is possible. | covered | Both run modes use pass loops and stop when no ready/progress exists at `crates/rhei-cli/src/main.rs:8442`, `crates/rhei-cli/src/main.rs:9508`, `crates/rhei-cli/src/main.rs:9645`, and `crates/rhei-cli/src/main.rs:9765`. Exit-code behavior is audited separately. |
| 57 | Mode selection uses orchestrated subprocess execution when any reachable non-terminal, non-gating state declares `program`, `agent`, `target`, `all_targets`, `model`, or `all_models`. | covered | `run_command` skips terminal/gating states and calls `state_declares_autonomous_execution` at `crates/rhei-cli/src/main.rs:8346`; the predicate checks all six fields at `crates/rhei-cli/src/main.rs:10203`. |
| 57 | Callback-only advancement is entered only when no autonomous state exists, or when spawning is explicitly disabled with `--no-agent` and/or `--no-program`. | partial | No-autonomous plans dispatch to `run_callback_mode` at `crates/rhei-cli/src/main.rs:8357`. Explicit disable flags are applied per state inside `run_agent_mode` at `crates/rhei-cli/src/main.rs:8495` and `crates/rhei-cli/src/main.rs:8507`, rather than selecting `run_callback_mode` globally. The behavior is mostly covered, but the mode-selection contract is only partially literal. |
| 57 | Model/target-driven work with no resolvable agent transport fails with a missing-agent configuration error and does not silently fall back. | covered | Empty invocations return an error unless `--no-agent` is set at `crates/rhei-cli/src/main.rs:8507`; the missing-agent remediation error is built at `crates/rhei-cli/src/main.rs:8533`. |
| 59 | Load state machine and plan, then validate. | covered | `run_command` normalizes and loads the input, resolves the state machine, validates the plan, and returns validation errors at `crates/rhei-cli/src/main.rs:8318` and `crates/rhei-cli/src/main.rs:8339`. |
| 60 | Ready set includes only tasks whose `**Prior:**` dependencies are terminal. | covered | `find_ready_tasks` builds a dependency state map and checks all `task.prior` entries at `crates/rhei-cli/src/main.rs:10049` and `crates/rhei-cli/src/main.rs:10069`. |
| 60 | Ready set excludes terminal tasks. | covered | `find_ready_tasks` skips terminal states at `crates/rhei-cli/src/main.rs:10061`. |
| 60 | Ready set excludes gating tasks. | covered | `find_ready_tasks` skips states whose definition has `gating` at `crates/rhei-cli/src/main.rs:10063`. |
| 60 | Ready set requires current state's required `inputs:` to exist. | missing | `find_ready_tasks` only checks terminal/gating state and priors at `crates/rhei-cli/src/main.rs:10061` and `crates/rhei-cli/src/main.rs:10069`. Required inputs are checked elsewhere during transition entry at `crates/rhei-cli/src/main.rs:5412`, not during ready-set computation before spawn. |
| 60 | Ready set excludes poll states whose `metadata.tasks.<id>.pollNextAttemptAt.<state-name>` is later than now. | missing | `find_ready_tasks` has no poll or metadata deadline check at `crates/rhei-cli/src/main.rs:10043`. The runtime metadata update helper only writes `stateVisits` at `crates/rhei-cli/src/main.rs:4198`; no `pollNextAttemptAt` runtime path is present. |
| 61 | Up to `--parallel` tasks from the ready set execute concurrently. | partial | Agent batches honor `--parallel` at `crates/rhei-cli/src/main.rs:8910` and spawn threads at `crates/rhei-cli/src/main.rs:9218`. Program tasks are executed sequentially in a for-loop at `crates/rhei-cli/src/main.rs:8672`. |
| 61 | At most one ready task per non-concurrent state is scheduled per pass. | covered | The concurrent-state filter uses a `state_claimant` map and defers same-state tasks when `concurrent` is false at `crates/rhei-cli/src/main.rs:8870`. |
| 62 | Resolve each state's target as an agent subprocess or program. | covered | Program states resolve via `resolve_program` at `crates/rhei-cli/src/main.rs:8501`; agent invocations resolve via `resolve_agent_invocations` at `crates/rhei-cli/src/main.rs:8505`. |
| 63 | If the state declares `snapshot.inherit:`, resolve and preload the source snapshot before spawning the agent. | partial | Correctly placed call sites precede agent spawn at `crates/rhei-cli/src/main.rs:9022` and `crates/rhei-cli/src/main.rs:9274`. The hook body is a no-op that discards arguments and returns `Ok(())` at `crates/rhei-cli/src/main.rs:10368`, so no snapshot is resolved or preloaded. |
| 63 | Polling states reject `snapshot.inherit` in v1. | missing | Validator poll checks cover interval, max attempts, final/gating, visits, and self-loop at `crates/rhei-validator/src/lib.rs:948`, but there is no state `snapshot` field in `StateDef` near `crates/rhei-validator/src/lib.rs:485` and no poll/snapshot rejection in `validate_poll_configuration`. |
| 64 | Spawn subprocess with resolved instructions, `RHEI_*` environment variables, and timeout. | covered | Agent instructions are composed in `compose_agent_prompt` at `crates/rhei-cli/src/main.rs:7247`; agent env vars are set at `crates/rhei-cli/src/main.rs:7413`; program env vars are set at `crates/rhei-cli/src/main.rs:8081`; timeout resolution/spawn are at `crates/rhei-cli/src/main.rs:7835` and `crates/rhei-cli/src/main.rs:8138`. |
| 65 | Wait for subprocess exit or timeout. On timeout, send SIGTERM, wait 10 s, then SIGKILL. | covered | Agent wait/timeout handling sends SIGTERM and then kills after `AGENT_TERMINATE_GRACE` at `crates/rhei-cli/src/main.rs:7963`; non-test grace is 10 s at `crates/rhei-cli/src/main.rs:7732`. Program timeout handling uses SIGTERM, 10 s sleep, and kill at `crates/rhei-cli/src/main.rs:8183`. |
| 66 | Completion condition is exit code 0 plus every required `outputs:` artifact on disk. | partial | Agent success requires `status.success()` before auto-advance at `crates/rhei-cli/src/main.rs:9110`; output presence is checked by `state_outputs_exist_for_resolved_invocation` at `crates/rhei-cli/src/main.rs:6987` and pending-output gating at `crates/rhei-cli/src/main.rs:9110`. If outputs are missing, the implementation warns and stalls rather than routing through error handling at `crates/rhei-cli/src/main.rs:9148`. |
| 67-69 | Select outgoing transition without applying it yet; on success select the first declared transition whose `condition` / `exit_code` matches. | partial | Agent success uses `find_next_transition` before `execute_transition` at `crates/rhei-cli/src/main.rs:10284`, and `find_next_transition` returns the first applicable exact/wildcard transition at `crates/rhei-cli/src/main.rs:10224`. Program exit-code transitions are selected by `find_program_exit_transition` at `crates/rhei-cli/src/main.rs:8238`, but multiple matching exit-code rules error instead of choosing the first declared rule at `crates/rhei-cli/src/main.rs:8265`. |
| 69-71 | On non-zero exit or missing outputs, route through the state's error or timeout transition. | partial | Timeout transitions are handled by `fire_timeout_transition` at `crates/rhei-cli/src/main.rs:9940`; program `exit_code` / `nonzero` transitions are handled at `crates/rhei-cli/src/main.rs:8279`. Agent non-zero exits without timeout do not look for a generic error transition and instead error/continue at `crates/rhei-cli/src/main.rs:9193`. Missing outputs warn/stall at `crates/rhei-cli/src/main.rs:9148`. |
| 72-73 | With no error transition and `--continue-on-error` unset, abort non-zero. | partial | Agent/program non-zero errors return `Err` when `--continue-on-error` is unset at `crates/rhei-cli/src/main.rs:8837`, `crates/rhei-cli/src/main.rs:9199`, and `crates/rhei-cli/src/main.rs:9488`. Missing-output failure does not abort; it warns at `crates/rhei-cli/src/main.rs:9148` and the run can end with `Ok(())` at `crates/rhei-cli/src/main.rs:9578`. |
| 74-79 | For agent-bearing states with supported snapshot sessions, write auto `_state` and matching named `snapshot.emit:` after transition selection and before transition application. | partial | Ordering is wired: `emit_snapshots_after_transition_selection` is called between `find_next_transition` and `execute_transition` at `crates/rhei-cli/src/main.rs:10284` and `crates/rhei-cli/src/main.rs:10309`. The hook itself is a no-op at `crates/rhei-cli/src/main.rs:10337`, so no snapshots are written. |
| 76-78 | Poll self-loop attempts do not emit snapshots; terminal poll exits may emit. | missing | The emit hook only discards `(current_state, selected_to_state)` at `crates/rhei-cli/src/main.rs:10343`; no poll self-loop suppression or terminal-exit emit behavior is implemented. |
| 80 | Apply the selected transition. | covered | `try_auto_advance_task` applies the selected transition with `execute_transition` after the snapshot hook at `crates/rhei-cli/src/main.rs:10309`. |
| 80-81 | Subprocess must not call `rhei transition` or `rhei complete`; orchestrator owns transition. | partial | The agent prompt includes the prohibition at `crates/rhei-cli/src/main.rs:7300`, and orchestrator code applies transitions at `crates/rhei-cli/src/main.rs:10309`. Program subprocesses receive no comparable instruction/enforcement, and no runtime guard can prevent external CLI calls during execution. |
| 82 | Repeat until no pass makes progress. | covered | Agent mode breaks only when ready is empty or `advanced_any` is false at `crates/rhei-cli/src/main.rs:8444` and `crates/rhei-cli/src/main.rs:9508`; callback mode mirrors this at `crates/rhei-cli/src/main.rs:9647` and `crates/rhei-cli/src/main.rs:9765`. |
| 82 | Exit 0 when every task is terminal. | covered | Natural completion returns `Ok(())` from run modes at `crates/rhei-cli/src/main.rs:9578` and `crates/rhei-cli/src/main.rs:9808`, which maps to process success. |
| 82 | Exit non-zero when progress halts with non-terminal tasks remaining and no further advancement is possible. | missing | Run modes return `Ok(())` on natural loop exit regardless of remaining non-terminal task count at `crates/rhei-cli/src/main.rs:9578` and `crates/rhei-cli/src/main.rs:9808`. Summaries may print "No tasks could be advanced" at `crates/rhei-cli/src/main.rs:9522`, but the process still succeeds. |

## 4. Gating

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 84 | `rhei run` does not transition out of gating states; exiting one requires explicit human `rhei transition`. | covered | Gating tasks are excluded from ready work at `crates/rhei-cli/src/main.rs:10061`, so run does not select transitions from them. |
| 86-92 | Gating states are a barrier, not an immediate global abort; independent non-gating work continues until only gating or gating-blocked work remains. | covered | `find_ready_tasks` excludes only the gating task itself while still returning other tasks with satisfied priors at `crates/rhei-cli/src/main.rs:10058`. A gating dependency is not terminal, so `dependency_is_satisfied` keeps downstream work blocked at `crates/rhei-cli/src/main.rs:10035`. |

## 5. Dry Run

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 96 | `--dry-run` performs the same scan and selection logic but prints planned transitions instead of executing subprocesses or callbacks. | partial | Callback dry-run computes `find_next_transition` and prints at `crates/rhei-cli/src/main.rs:8595` and `crates/rhei-cli/src/main.rs:8598`; callback-only mode does similarly at `crates/rhei-cli/src/main.rs:9688`. Agent/program dry-run prints spawn plans at `crates/rhei-cli/src/main.rs:8661` and `crates/rhei-cli/src/main.rs:8943`, not the spec's planned transition line. |
| 98-100 | Dry-run output format is exactly `would transition: Task <ID>  <from> -> <to>`. | missing | Implemented strings are `Would transition Task ... from ... to ...` at `crates/rhei-cli/src/main.rs:8598` and `crates/rhei-cli/src/main.rs:9695`, plus `Would spawn` messages for subprocess states at `crates/rhei-cli/src/main.rs:8661` and `crates/rhei-cli/src/main.rs:8943`. |
| 102 | Dry-run acquires no file lock. | covered | Dry-run branches continue/break before `execute_transition`, whose locks are acquired at `crates/rhei-cli/src/main.rs:5133`. |
| 102 | Dry-run rewrites no markdown. | covered | Dry-run branches skip `execute_transition`; atomic writes occur only in that path at `crates/rhei-cli/src/main.rs:5443`. |
| 102 | Dry-run creates no runtime artifacts. | missing | Both run modes start the frontend before dry-run branching at `crates/rhei-cli/src/main.rs:8383` and `crates/rhei-cli/src/main.rs:9594`. Frontend selection opens/creates `runtime/transitions.log` at `crates/rhei-tui/src/frontend.rs:51`, and `JournalSink::open` creates the `runtime` directory/file at `crates/rhei-tui/src/journal.rs:24`. |

## 6. Parallel Execution

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 106 | With `--parallel N`, up to `N` subprocesses run concurrently. | partial | Agent subprocesses are threaded in the parallel branch at `crates/rhei-cli/src/main.rs:9218`. Program subprocesses are not; they run sequentially at `crates/rhei-cli/src/main.rs:8672`. |
| 108 | Orchestrator assigns each spawn a slot index. | covered | Sequential agent/program slots use 0 at `crates/rhei-cli/src/main.rs:8700` and `crates/rhei-cli/src/main.rs:9037`; parallel agents use `slot_idx` at `crates/rhei-cli/src/main.rs:9285`. |
| 109 | Writes one journal line per `SlotAssigned` and one per `SlotReleased`. | covered | Run emits slot events at `crates/rhei-cli/src/main.rs:8700`, `crates/rhei-cli/src/main.rs:8729`, `crates/rhei-cli/src/main.rs:9037`, `crates/rhei-cli/src/main.rs:9081`, `crates/rhei-cli/src/main.rs:9289`, and `crates/rhei-cli/src/main.rs:9344`. `JournalSink` writes assignment/release lines at `crates/rhei-tui/src/journal.rs:75` and `crates/rhei-tui/src/journal.rs:90`. |
| 110 | Every state write is serialized through its own file lock. | covered | `execute_transition` locks metadata and task files before reading/writing at `crates/rhei-cli/src/main.rs:5133`. |
| 112 | Tasks whose transitions would race on the same task node are never scheduled in parallel; ready set excludes tasks already in flight. | partial | A pass joins all agent handles before the next pass at `crates/rhei-cli/src/main.rs:9361`, so no later ready-set scan overlaps an earlier pass. There is no explicit in-flight marker in the ready set at `crates/rhei-cli/src/main.rs:10043`, and fanout invocations for one task can be spawned together by design at `crates/rhei-cli/src/main.rs:8870`. |

## 7. Polling States

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 116 | A poll state is time-triggered: each attempt spawns one subprocess and evaluates transitions. | partial | Since `poll` is parsed into `StateDef` at `crates/rhei-validator/src/lib.rs:511`, normal agent/program spawn still occurs. The time-triggered attempt lifecycle is not implemented in the run loop. |
| 116 | A self-loop transition from a poll state means "retry after `poll.interval`". | missing | `find_next_transition` treats self-loops like ordinary transitions at `crates/rhei-cli/src/main.rs:10224`; there is no special poll interval scheduling path. |
| 118 | Between attempts, persist `metadata.tasks.<id>.pollNextAttemptAt.<state-name> = now() + interval`. | missing | Runtime metadata updates only maintain `stateVisits` for visit-limited states at `crates/rhei-cli/src/main.rs:4198`; no poll deadline is written. |
| 118 | Persist `metadata.tasks.<id>.stateVisits.<state-name>` as attempt counter. | missing | `state_visit_limit` returns only `visits`, not `poll.max_attempts`, at `crates/rhei-cli/src/main.rs:3873`; poll states therefore do not enter the `stateVisits` update path at `crates/rhei-cli/src/main.rs:4198`. |
| 119 | Release the `--parallel` slot between poll attempts so other ready tasks may run. | missing | Because self-loop poll attempts are applied as normal transitions and no `pollNextAttemptAt` deferral is written, there is no distinct poll slot-release lifecycle beyond ordinary subprocess completion at `crates/rhei-cli/src/main.rs:9081` / `crates/rhei-cli/src/main.rs:9344`. |
| 120 | Do not hold a timer thread; next pass re-scans and only picks the task once `pollNextAttemptAt` is in the past. | missing | The ready-set scan has no `pollNextAttemptAt` filter at `crates/rhei-cli/src/main.rs:10043`, and the run loop breaks when ready is empty at `crates/rhei-cli/src/main.rs:8444` rather than maintaining poll deadlines. |
| 122 | If all remaining non-terminal tasks are gating, gating-blocked, or poll-blocked, sleep until earliest poll deadline, bounded below by 1 s. | missing | Agent and callback loops break immediately when `ready.is_empty()` at `crates/rhei-cli/src/main.rs:8444` and `crates/rhei-cli/src/main.rs:9647`; no sleep-until-earliest-deadline logic exists. |
| 122 | If no poll deadline is pending and only gating remains, exit as today. | covered | With no ready tasks, run loops exit at `crates/rhei-cli/src/main.rs:8444` and `crates/rhei-cli/src/main.rs:9647`. |
| 124 | Once `stateVisits.<state>` reaches `poll.max_attempts`, refuse self-loop and pick first matching non-self-loop. | missing | Self-loop refusal is driven by `loop_reentry_allowed`, which consults only `state_visit_limit`; `state_visit_limit` ignores `poll.max_attempts` at `crates/rhei-cli/src/main.rs:3873`, and `transition_rule_is_applicable` uses that result at `crates/rhei-cli/src/main.rs:4077`. |
| 124 | If no non-self-loop matches after poll exhaustion, halt task with "polling exhausted with no matching non-self-loop transition"; `--continue-on-error` applies. | missing | No polling-exhausted error string or branch exists in the run loop; transition applicability has no poll-exhaustion path at `crates/rhei-cli/src/main.rs:4069`. |
| 124 | A non-self-loop exit clears both `pollNextAttemptAt.<state>` and `stateVisits.<state>`. | missing | `execute_transition` updates visit metadata for target states at `crates/rhei-cli/src/main.rs:5388`; no poll deadline or per-state visit clearing is performed on non-self-loop poll exit. |
| 126 | `snapshot.inherit` is rejected on polling states in v1. | missing | Poll validator checks do not mention snapshots at `crates/rhei-validator/src/lib.rs:948`, and `StateDef` has no parsed snapshot field near `crates/rhei-validator/src/lib.rs:485`. |
| 126-128 | Snapshot emit, including auto-emit, is suppressed for poll self-loop attempts and runs only on terminal non-self-loop exit when otherwise snapshot-capable. | missing | The emit hook is a no-op and only discards `(current_state, selected_to_state)` at `crates/rhei-cli/src/main.rs:10337`; no poll-specific emit behavior exists. |

## 8. Concurrent Vs. Serial States

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 132-135 | `concurrent: true` allows any number of ready tasks in the same state to schedule together, bounded by `--parallel`; `false` default schedules at most one per pass. | covered | `StateDef.concurrent` defaults false at `crates/rhei-validator/src/lib.rs:505`; the run filter allows concurrent states and defers non-concurrent same-state tasks at `crates/rhei-cli/src/main.rs:8870`. |
| 137 | `concurrent` does not change state entry/exit semantics or transitions. | covered | The concurrent-state code only filters the batch before spawning at `crates/rhei-cli/src/main.rs:8870`; transition application still goes through the normal `execute_transition` path at `crates/rhei-cli/src/main.rs:10309`. |
| 137 | `concurrent` does not affect within-task fanout; every resolved invocation for one scheduled task is still spawned together. | covered | The filter explicitly keeps fanout invocations from the same task together: same claimant entries are pushed even in non-concurrent states at `crates/rhei-cli/src/main.rs:8886`. |

## 9. Relationship To Other Commands

| Spec lines | Claim | Status | Evidence / gap |
|---|---|---|---|
| 141 | `rhei run` drives the full plan under orchestrator authority. | covered | Run applies transitions through `execute_transition` after subprocess completion at `crates/rhei-cli/src/main.rs:10309`. |
| 141 | `rhei run` is mutually exclusive per execution with manual-worker flow (`next` / `transition` / `complete`); they never overlap on the same task because run holds transition responsibility. | partial | Final state writes are compare-and-swap protected and locked at `crates/rhei-cli/src/main.rs:5179` and `crates/rhei-cli/src/main.rs:5133`. However, `rhei run` does not claim or mark tasks in-flight during subprocess execution, and `find_ready_tasks` ignores assignee/in-flight state at `crates/rhei-cli/src/main.rs:10043`. A manual command can race until the final CAS detects a changed state. |
| 143 | See usage spec for the command comparison table. | not-normative | Cross-reference only. |
| 145-153 | Related-specification list. | not-normative | Cross-reference list only. |
