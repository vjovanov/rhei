# Aggregated Completeness Gaps: impl-rhei-snapshots

Spec: `docs/functional-spec/rhei-snapshots.spec.md`

Inputs:

- `runtime/completeness/impl-rhei-snapshots-gaps-codex-xhigh-openai-gpt-5-5.md`

Aggregation rule: retained items marked `missing` or `partial` by any reviewer unless all reviewers marked the same requirement covered with concrete evidence. Where reviewers disagree, the disagreement is preserved for `completeness-fix`.

There was only one reviewer inventory for this task, so there are no cross-reviewer disagreements to preserve.

Retained gaps: 79. Dropped covered items: 26. Dropped non-normative items: 2.

## Context

- `CONTEXT-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:3-7`): Session snapshots capture an agent transcript at supported agent-bearing state exits for inheritance, ancestor branching, operator analysis, and cache benefits. Evidence: Runtime emission is only a no-op hook: `emit_snapshots_after_transition_selection` discards all arguments and does not write snapshots at `crates/rhei-cli/src/main.rs:12411` and `crates/rhei-cli/src/main.rs:12417`. Preload is also a stub at `crates/rhei-cli/src/main.rs:12442` and `crates/rhei-cli/src/main.rs:12463`.
- `CONTEXT-002` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:10-20`): Auto and named snapshots share storage/manifest schema; auto snapshots exist for `snapshot continue`; named snapshots implement authored lineage. Evidence: Cache readers assume a common manifest/storage shape via `SnapshotRecord` at `crates/rhei-cli/src/main.rs:8869` and `snapshot_record_from_manifest` at `crates/rhei-cli/src/main.rs:9102`; gap: neither auto nor named runtime writes exist, per the no-op emission hook at `crates/rhei-cli/src/main.rs:12411`.
- `CONTEXT-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:22-28`): Plans without `snapshot.emit` or `snapshot.inherit` still receive best-effort auto snapshots under `.rhei/cache/snapshots/`; default redactor is null unless configured. Evidence: Default cache dir is `.rhei/cache/snapshots` at `crates/rhei-cli/src/main.rs:6196`; `SnapshotSettings.redactor` defaults to `None` at `crates/rhei-cli/src/main.rs:6158`; gap: auto-emission itself is not implemented (`crates/rhei-cli/src/main.rs:12411`).

## Snapshot Model

- `MODEL-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:72-79`): A snapshot identity is `(task_id, snapshot_name, emitting_state, visit, target_slug, generation)`. Evidence: `SnapshotRecord` stores all six fields at `crates/rhei-cli/src/main.rs:8873`; `display_ref` renders all six at `crates/rhei-cli/src/main.rs:8906`; gap: runtime snapshot creation does not allocate or persist these identities because emission is a no-op at `crates/rhei-cli/src/main.rs:12411`.
- `MODEL-002` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:81-93`): Identity components have the specified meanings; `visit` is 1 for uncounted states and `generation` starts at 1 and increments on re-execution. Evidence: Cache readers read `visit` and `generation` from manifests with fallback defaults at `crates/rhei-cli/src/main.rs:9114` and `crates/rhei-cli/src/main.rs:9115`; gap: no writer computes visit values or generation counters at runtime (`crates/rhei-cli/src/main.rs:12411`).
- `MODEL-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:95-96`): Snapshots are immutable; re-running a state writes a new generation and updates `current` instead of overwriting. Evidence: GC refreshes `current` with symlink replacement at `crates/rhei-cli/src/main.rs:9674` and `crates/rhei-cli/src/main.rs:9713`; gap: the actual snapshot writer, generation allocation, and immutable write path are absent (`crates/rhei-cli/src/main.rs:12406`).

## Auto And Named Emit

- `AUTO-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:100-107`): Auto snapshots are emitted at every agent-bearing state exit, exclude final/gating/program states, use reserved `_state`, fire on failures/timeouts, and are not inherit candidates. Evidence: Runtime auto-emission is not implemented; the emission hook is deliberately empty at `crates/rhei-cli/src/main.rs:12411` and `crates/rhei-cli/src/main.rs:12417`.
- `AUTO-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:109-117`): Named snapshots are emitted only when a state declares `snapshot.emit`; named and auto snapshots are stored under independent identities and both may write transcript copies. Evidence: `snapshot.emit` parses and validates, but `emit_snapshots_after_transition_selection` performs no write at `crates/rhei-cli/src/main.rs:12411` and `crates/rhei-cli/src/main.rs:12417`.
- `AUTO-004` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:119-124`): Auto-emit silently skips agents without supported `SessionLayout`; named-emit fails with `unsupported-snapshot-session`. Evidence: Named-emit settings validation errors when the agent lacks a supported session layout at `crates/rhei-cli/src/main.rs:6771` and `crates/rhei-cli/src/main.rs:6776`; gap: runtime auto-skip and named emit failure paths are absent because emission is a no-op at `crates/rhei-cli/src/main.rs:12411`.

## State-Machine YAML

- `YAML-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:128-130`): Auto-emit requires no state-machine configuration; `snapshot.emit` and `snapshot.inherit` are optional named-lineage fields. Evidence: State snapshot fields are optional at `crates/rhei-validator/src/lib.rs:594`; gap: auto-emit behavior is missing at runtime (`crates/rhei-cli/src/main.rs:12411`).
- `YAML-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:147`): `snapshot.emit.on` accepts `success`, `failure`, or `always`; default is `success`. Evidence: Accepted values are checked at `crates/rhei-validator/src/lib.rs:1061`; gap: no runtime emits exist, so the default and matching behavior are not applied (`crates/rhei-cli/src/main.rs:12411`).
- `YAML-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:147-158`): `emit.on: success` depends on exit code 0 and required outputs; `failure` includes timeouts; timeout is distinguished by manifest `completion`. Evidence: Completion classification and named emit policy are not implemented in the snapshot writer; the hook is empty at `crates/rhei-cli/src/main.rs:12411`.
- `YAML-005` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:149-151`): States without `snapshot.emit` produce no named snapshot while still following auto-emit best effort. Evidence: There is no runtime writer for either named or auto snapshots; see `crates/rhei-cli/src/main.rs:12411`.
- `YAML-006` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:179-185`): `snapshot.inherit.name` is required; `from`, `compat`, `required`, and `select` are optional with defaults `self`, `native`, `false`, and default selectors. Evidence: Required `name` and optional fields are parsed at `crates/rhei-validator/src/lib.rs:613`; accepted `from` and `compat` values are validated at `crates/rhei-validator/src/lib.rs:1090` and `crates/rhei-validator/src/lib.rs:1100`; gap: runtime defaults are not applied because preload resolution is a no-op at `crates/rhei-cli/src/main.rs:12463`.
- `YAML-007` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:181`): `snapshot.inherit.name` must use the snapshot name to look up. Evidence: Static analysis compares emit names to inherit names at `crates/rhei-validator/src/lib.rs:1183`; gap: runtime lookup is not implemented (`crates/rhei-cli/src/main.rs:12463`).
- `YAML-008` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:182`): `from` supports only `self` and `ancestor`; default is `self`. Evidence: Value validation is implemented at `crates/rhei-validator/src/lib.rs:1090`; gap: self and ancestor runtime walks are not implemented (`crates/rhei-cli/src/main.rs:12463`).
- `YAML-009` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:183`): `compat` supports `native` and `none`; default is `native`; `none` never preloads. Evidence: Value validation is implemented at `crates/rhei-validator/src/lib.rs:1100`; gap: runtime `compat: none` skip behavior is absent (`crates/rhei-cli/src/main.rs:12463`).
- `YAML-010` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:184`): `required: false` downgrades missing/incompatible/unsupported preload to warnings and cold start; `true` fails before spawn. Evidence: `required: true` with no preload strategy is rejected in settings validation at `crates/rhei-cli/src/main.rs:6784`; gap: runtime missing, incompatible, unsupported, and warning/cold-start paths are absent (`crates/rhei-cli/src/main.rs:12463`).
- `YAML-014` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:191-200`): `select.target: same` resolves per inheritor target and is slug-exact, including mode. Evidence: `same` is accepted only when the inheriting state declares a target shape at `crates/rhei-validator/src/lib.rs:1155`; target slug normalization includes mode through selector rendering at `crates/rhei-validator/src/lib.rs:390`; gap: runtime per-invocation `same` resolution is not implemented (`crates/rhei-cli/src/main.rs:12463`).
- `YAML-016` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:205-216`): Selector defaults and fanout requirements follow the table; integer selectors are 1-based and `>= 1`. Evidence: Fanout omission is rejected at `crates/rhei-validator/src/lib.rs:1214`; integer selectors require `>= 1` at `crates/rhei-validator/src/lib.rs:667`; gap: runtime default visit/generation and per-invocation fanout resolution are not implemented (`crates/rhei-cli/src/main.rs:12463`).

## Lineage Resolution

- `LINEAGE-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:220-238`): `from: self` runtime resolution filters prior same-task history by name, state, target, visit, generation; zero candidates fall back, one resolves, multiple error. Evidence: `preload_snapshot_inherit_before_spawn` does not resolve candidate history or inspect snapshots; it discards inputs and returns `Ok(())` at `crates/rhei-cli/src/main.rs:12463`.
- `LINEAGE-002` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:240-246`): A state may both emit and inherit the same name; visit `N` inherits the prior visit and visit 1 follows missing-snapshot rules. Evidence: The validator permits independent emit/inherit fields, but runtime prior-visit resolution is absent in the preload stub at `crates/rhei-cli/src/main.rs:12463`.
- `LINEAGE-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:248-258`): `from: ancestor` walks parent upward, stops at nearest matching ancestor, applies post-filters, and errors on ambiguity. Evidence: No runtime ancestor walk exists in `preload_snapshot_inherit_before_spawn` at `crates/rhei-cli/src/main.rs:12463`; static validation also does not know the task hierarchy, only all states in the machine at `crates/rhei-validator/src/lib.rs:1183`.
- `LINEAGE-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:260-266`): Counted loops emit one snapshot per visit; `latest` picks the highest-numbered visit, and failed visits without a matching emit policy are skipped. Evidence: No runtime emission or lookup exists; `emit_snapshots_after_transition_selection` is empty at `crates/rhei-cli/src/main.rs:12411`.

## Timing

- `TIMING-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:268-285`): Inheritance happens immediately before the state invocation; emit happens after completion/transition selection and before applying the transition; inherit is a property of the state. Evidence: Preload hook is called before sequential spawn at `crates/rhei-cli/src/main.rs:10728` and before parallel spawn at `crates/rhei-cli/src/main.rs:11029`; emit hook is called before transition application at `crates/rhei-cli/src/main.rs:12368`; gap: both hooks are no-op stubs (`crates/rhei-cli/src/main.rs:12411`, `crates/rhei-cli/src/main.rs:12463`).

## Fallbacks

- `FALLBACK-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:300-305`): `required: false` has exactly one fallback: run cold; it does not try another name, farther ancestor, or another emitting state. Evidence: Runtime fallback handling is absent because `preload_snapshot_inherit_before_spawn` does not resolve snapshots and always returns success after checking only `--from-snapshot` gating at `crates/rhei-cli/src/main.rs:12449` and `crates/rhei-cli/src/main.rs:12463`.

## Compatibility

- `COMPAT-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:309-328`): `native_compatible`, `snapshot_layout_matches`, and `cache_beneficial` are independent predicates with the specified fields. Evidence: No `native_compatible`, `snapshot_layout_matches`, or `cache_beneficial` implementation exists; the only matching text is documentation in the preload stub at `crates/rhei-cli/src/main.rs:12428`.
- `COMPAT-002` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:330-343`): Layout compatibility must compare `SessionLayout` kind and resume-affecting fields for `FlatById` and `PerProjectJson`; cross-agent inheritance is never natively compatible. Evidence: Session profiles are stored as untyped JSON at `crates/rhei-validator/src/lib.rs:197`; runtime layout comparison is absent, and preload is a no-op at `crates/rhei-cli/src/main.rs:12463`.
- `COMPAT-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:345-350`): Mode is not part of `native_compatible`; stricter mode matching is expressed with slug-exact `select.target`. Evidence: No native compatibility predicate is implemented; preload does not inspect mode or target slugs at `crates/rhei-cli/src/main.rs:12463`.
- `COMPAT-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:352-357`): `compat: native` gates preload on native compatibility; `cache_beneficial` is advisory and logged; `compat: none` disables preload. Evidence: Runtime compat evaluation and cache-benefit logging are absent; the preload function discards `opts` and `resolved` at `crates/rhei-cli/src/main.rs:12463`.
- `COMPAT-005` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:358-362`): Authored inherit cannot preload `completion: timeout`; required true errors, required false warns/runs cold; CLI continue warns but may proceed. Evidence: `snapshot continue` warns on timeout manifests at `crates/rhei-cli/src/main.rs:9748`; gap: authored inherit timeout rejection is not implemented (`crates/rhei-cli/src/main.rs:12463`), and continue ultimately returns a deferred unsupported error at `crates/rhei-cli/src/main.rs:9771`.
- `COMPAT-006` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:364-367`): Pi observed provider/model are parsed from the session JSONL header; other agents record both declared and observed values. Evidence: No emit path parses or records observed provider/model; emission is a no-op at `crates/rhei-cli/src/main.rs:12411`.

## Subtasks And Lineage Edges

- `SUBTASK-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:371-386`): `from: ancestor` child tasks copy the transcript into the child's snapshot store as an independent branch; multiple children get distinct copies; no workspace crossing. Evidence: Runtime ancestor preload/copy is absent in `preload_snapshot_inherit_before_spawn` at `crates/rhei-cli/src/main.rs:12463`.
- `SUBTASK-002` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:388-405`): `parent_ref` is set only when a source was successfully preloaded; cold starts and no-inherit states use null; ancestor copies are staging artifacts. Evidence: Cache readers preserve manifest values, but no writer computes `parent_ref`; emission is empty at `crates/rhei-cli/src/main.rs:12411`.
- `SUBTASK-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:406-408`): Operator generations from `rhei snapshot continue` record the selected source snapshot in `parent_ref`. Evidence: `snapshot_continue_command` never writes an operator generation and returns a deferred unsupported error at `crates/rhei-cli/src/main.rs:9771`.
- `SUBTASK-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:410-421`): Lineage edges correspond to successful preloads only; `snapshot show` parent walks always land on actually-loaded snapshots; v1 has no `parent_refs` plural. Evidence: No successful preload tracking or parent traversal exists; `print_snapshot_show` just prints the manifest JSON at `crates/rhei-cli/src/main.rs:9234`.

## Storage Layout

- `STORAGE-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:423-438`): Snapshot storage path is `.rhei/cache/snapshots/<task-id>/<snapshot-name>/<emitting-state>/<visit>/<target.slug>/g<N>/manifest.json, transcript.<ext>, current`. Evidence: Cache readers and tests use that path shape at `crates/rhei-cli/src/main.rs:9680` and `crates/rhei-cli/src/main.rs:17371`; gap: runtime writes are absent (`crates/rhei-cli/src/main.rs:12411`).
- `STORAGE-002` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:440-446`): Path segments use task id encoding, `_state` or authored name, canonical unsuffixed state, visit never 0, and generation `g<N>` starts at 1. Evidence: Readers parse task/name/state/visit/generation from manifests and paths at `crates/rhei-cli/src/main.rs:9110`; name validation forbids `_state` for authored names at `crates/rhei-validator/src/lib.rs:642`; gap: no writer enforces visit/generation during emission (`crates/rhei-cli/src/main.rs:12411`).
- `STORAGE-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:448-449`): `current` is a relative symlink and updated atomically via temporary symlink rename. Evidence: GC refresh writes a temporary symlink then renames it at `crates/rhei-cli/src/main.rs:9713`; gap: the orchestrator snapshot writer does not exist, so normal current updates are missing (`crates/rhei-cli/src/main.rs:12411`).
- `STORAGE-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:451-452`): The cache root is gitignored by default; committing selected snapshots is outside `rhei run`. Evidence: `.gitignore` lists `.agents/scratchpad/`, `target/`, package artifacts, and OS files, but no `.rhei/cache/` entry at `.gitignore:1`.

## Target Slugs

- `SLUG-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:471-472`): Raw selector string is recorded in `manifest.target.selector`. Evidence: No runtime manifest writer exists; emission is a no-op at `crates/rhei-cli/src/main.rs:12411`.

## Atomic Writes And Reads

- `ATOMIC-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:483-503`): Snapshot generation writes take an advisory lock, allocate the smallest unused generation, stage under `g<N>.tmp-<nonce>`, copy transcript, write/verify manifest SHA-256, rename atomically, and update `current` only for orchestrator generations. Evidence: No snapshot writer or SHA-256 path is implemented; `rg` finds no production `transcript_sha256` writer, and the emission hook is empty at `crates/rhei-cli/src/main.rs:12411`.
- `ATOMIC-002` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:504-506`): Writers may remove stale temporary directories only when safe; finalized `g<N>` directories must not be deleted. Evidence: No writer/stale-temp cleanup exists; runtime emission is empty at `crates/rhei-cli/src/main.rs:12411`.
- `ATOMIC-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:508-518`): Readers must avoid partial reads and resolve `current` once with `realpath` when reading manifest and transcript together. Evidence: `resolve_snapshot_path_ref` canonicalizes explicit generation paths at `crates/rhei-cli/src/main.rs:9334`; `snapshot_record_from_manifest` reads concrete `g<N>/manifest.json` paths at `crates/rhei-cli/src/main.rs:9102`; gap: shorthand current resolution scans records and marks `is_current` rather than resolving `current` once for manifest+transcript atomicity at `crates/rhei-cli/src/main.rs:9280`.
- `ATOMIC-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:520-524`): If finalizing `g<N>` finds an existing destination, the writer retries with the next generation and rewrites the manifest. Evidence: No finalization/retry writer is implemented; emission is empty at `crates/rhei-cli/src/main.rs:12411`.

## Manifest

- `MANIFEST-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:526-606`): `manifest.json` version 1 has the listed required fields and field types. Evidence: `snapshot_record_from_manifest` reads a subset of fields at `crates/rhei-cli/src/main.rs:9110`; gap: it silently defaults missing `visit`, `generation`, `transcript_bytes`, and `produced_by` at `crates/rhei-cli/src/main.rs:9114`, `crates/rhei-cli/src/main.rs:9115`, `crates/rhei-cli/src/main.rs:9141`, and `crates/rhei-cli/src/main.rs:9144`, and no writer creates full manifests.
- `MANIFEST-002` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:581-606`): Manifest validation requires path-matching identity fields, visit/generation >= 1, target selector/slug/resolved, provider/model fields, session id/layout, transcript path/hash/bytes, parent_ref, created_at, completion, and produced_by. Evidence: The implementation validates only `completion`/`produced_by` combinations at `crates/rhei-cli/src/main.rs:9163`; it does not validate the full schema or path-component consistency.
- `MANIFEST-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:603`): `parent_ref` is object or null and contains the full parent identity tuple when set. Evidence: Manifest JSON is preserved and printed by `print_snapshot_show` at `crates/rhei-cli/src/main.rs:9234`; gap: no validation or writer computes `parent_ref`.
- `MANIFEST-005` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:606`): `produced_by` is `orchestrator` or `operator`; operator emissions never advance `current`. Evidence: `produced_by` is validated at `crates/rhei-cli/src/main.rs:9168`; GC current refresh only considers orchestrator records at `crates/rhei-cli/src/main.rs:9685`; gap: no operator emission path exists (`crates/rhei-cli/src/main.rs:9771`).

## Agent Profiles

- `AGENT-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:610-670`): `CustomAgentProfile.session` is an optional typed `AgentSessionProfile` with resume/fork/interactive/id/session flags and layout strategy enums. Evidence: `CustomAgentProfile.session` exists, but it is `Option<serde_json::Value>` rather than the typed schema at `crates/rhei-validator/src/lib.rs:197`; CLI checks only key presence for layout/resume at `crates/rhei-cli/src/main.rs:6810`.
- `AGENT-002` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:672-676`): Profile strategy is explicit; `ResumeStrategy::None` makes preload impossible and optional inherit gets a validation hint. Evidence: `profile_has_snapshot_resume` treats `"none"` as no preload at `crates/rhei-cli/src/main.rs:6818`; required inherit errors when no preload strategy at `crates/rhei-cli/src/main.rs:6784`; gap: optional inherit warning/hint is not implemented.
- `AGENT-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:678-681`): `session_dir_flag` redirects agent session output into the active staging dir when provided. Evidence: No snapshot staging or session flag application exists; `build_agent_command` does not reference session profile fields, and preload/emit hooks are empty at `crates/rhei-cli/src/main.rs:12411` and `crates/rhei-cli/src/main.rs:12463`.
- `AGENT-004` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:683-693`): Built-in profile snapshot capabilities match the table, including no active session block for Claude/Codex, unsupported Cursor/Kilocode/Gemini, and Pi native continue/fork/session-dir layout. Evidence: Built-ins for Claude, Codex, Gemini, Cursor, Kilocode, and Pi all use `..Default::default()` with no `session` block at `crates/rhei-cli/src/main.rs:6296`, `crates/rhei-cli/src/main.rs:6313`, `crates/rhei-cli/src/main.rs:6334`, `crates/rhei-cli/src/main.rs:6349`, `crates/rhei-cli/src/main.rs:6364`, and `crates/rhei-cli/src/main.rs:6381`; gap: the spec's Pi session profile is not populated.
- `AGENT-005` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:699-706`): Emit requires only `SessionLayout`; preload requires `SessionLayout` plus non-`None` `ResumeStrategy`. Evidence: `profile_has_snapshot_layout` and `profile_has_snapshot_preload` encode this distinction at `crates/rhei-cli/src/main.rs:6810`; gap: no actual emit/preload runtime uses the profile.
- `AGENT-006` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:708-714`): No session/layout is unsupported for emit and preload; layout plus `ResumeStrategy::None` is unsupported for preload only; required false runs cold, required true fails, unsupported emit fails spawn. Evidence: Settings validation rejects named emit without layout and required inherit without preload at `crates/rhei-cli/src/main.rs:6771` and `crates/rhei-cli/src/main.rs:6784`; gap: runtime optional cold-start warnings and spawn-time failures are absent.
- `AGENT-007` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:716-719`): Cursor and Kilocode built-ins have no snapshot profile; auto-emit skips, explicit emit or required preload fails unless replaced by custom session block. Evidence: Built-ins have no session block at `crates/rhei-cli/src/main.rs:6349` and `crates/rhei-cli/src/main.rs:6364`; named emit/required preload validation is generic at `crates/rhei-cli/src/main.rs:6771`; gap: runtime auto-skip is not implemented.

## Pi

- `PI-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:723-741`): Pi runtime inheritance stages a target generation, appends `--session-dir` and `--fork`, canonicalizes transcript, writes manifest/current, and parses observed provider/model from JSONL. Evidence: Built-in Pi has no session profile at `crates/rhei-cli/src/main.rs:6381`; runtime staging and emit/preload are absent (`crates/rhei-cli/src/main.rs:12411`, `crates/rhei-cli/src/main.rs:12463`).

## Gemini

- `GEMINI-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:743-756`): Gemini snapshot support is unsupported in v1; built-in Gemini has no supported layout/resume and explicit operations fail. Evidence: Built-in Gemini has no `session` block at `crates/rhei-cli/src/main.rs:6334`; named emit and required preload validation fail for missing support at `crates/rhei-cli/src/main.rs:6771`; gap: runtime auto-skip/explicit spawn-time failure is absent.

## Claude

- `CLAUDE-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:758-770`): Claude Code built-in leaves `session` unset; inheritance runs cold unless required true fails before spawn. Evidence: Built-in Claude Code has no session block at `crates/rhei-cli/src/main.rs:6296`; required preload is rejected in settings validation at `crates/rhei-cli/src/main.rs:6784`; gap: optional runtime cold-start warning is absent.

## Codex

- `CODEX-001` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:772-792`): Codex built-in leaves `session` unset until spike; emit and inherit unsupported until layout/resume are known. Evidence: Built-in Codex has no session block at `crates/rhei-cli/src/main.rs:6313`; unsupported named emit/required preload validation is generic at `crates/rhei-cli/src/main.rs:6771`; gap: runtime auto-skip and optional cold-start behavior are absent.

## Runtime Behavior

- `RUN-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:796-823`): Spawn-time preload resolves lineage, handles missing required/optional, `compat: none`, timeout, native compatibility, cache benefit logging, unsupported profiles, stages session, and appends strategy flags before spawn. Evidence: `preload_snapshot_inherit_before_spawn` only rejects `--from-snapshot` when the state lacks `snapshot.inherit`, then discards inputs and returns `Ok(())` at `crates/rhei-cli/src/main.rs:12449` and `crates/rhei-cli/src/main.rs:12463`.
- `RUN-002` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:800-803`): Missing snapshot yields `missing-snapshot` for `required: true`, or logs "no snapshot found..." and cold-starts for `required: false`. Evidence: No snapshot lookup or missing-source branch exists in the preload stub at `crates/rhei-cli/src/main.rs:12463`.
- `RUN-003` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:804-805`): `compat: none` logs preload disabled and spawns cold. Evidence: No runtime compat handling exists; preload stub ignores `compat` at `crates/rhei-cli/src/main.rs:12463`.
- `RUN-004` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:806-812`): Timeout and incompatible snapshots fail or warn/cold-start according to `required`. Evidence: No manifest resolution, timeout check, or native compatibility check exists in the preload stub at `crates/rhei-cli/src/main.rs:12463`.
- `RUN-005` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:813-816`): `cache_beneficial` false logs provider/model/TTL reason and does not block preload. Evidence: No cache-benefit predicate or log path exists; preload stub is empty at `crates/rhei-cli/src/main.rs:12463`.
- `RUN-006` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:817-823`): Unsupported inheritor profile fails when required, otherwise warns/cold-starts; supported strategies stage and spawn with resume/fork flags. Evidence: Required preload without strategy is rejected during settings validation at `crates/rhei-cli/src/main.rs:6784`; gap: runtime optional warning/cold-start and flag/staging behavior are absent.
- `RUN-007` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:824-854`): Emit on exit classifies completion, writes auto snapshot always, writes named snapshot when `emit.on` matches, uses atomic writes, and duplicates transcript bytes under independent identities when both fire. Evidence: The emission hook is empty at `crates/rhei-cli/src/main.rs:12411`; no classification or write logic exists.
- `RUN-008` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:856-860`): Auto-emit is suppressed for final/gating/program states and both auto/named emit are suppressed on poll self-loop attempts. Evidence: Validator rejects explicit emit/inherit on final/gating/program where applicable at `crates/rhei-validator/src/lib.rs:1071`; no runtime emission exists, so suppression is not meaningfully implemented. The no-op hook comment says poll suppression is delegated but no module exists at `crates/rhei-cli/src/main.rs:12418`.

## Loops And Fanout

- `LOOP-001` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:866-877`): Counted-loop and fanout states emit separate snapshots per visit and per resolved target; `emit.on` applies per target; `all_models` normalizes through target tuple. Evidence: No runtime emission exists; `emit_snapshots_after_transition_selection` is empty at `crates/rhei-cli/src/main.rs:12411`.
- `LOOP-002` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:879-886`): Fanout inheritors resolve `select.target` per target, with explicit slug or `same`, and each invocation gets its own copy/generation directory. Evidence: Runtime fanout preload is not implemented; preload stub discards target information at `crates/rhei-cli/src/main.rs:12463`.
- `LOOP-003` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:888-892`): Polling states do not emit on self-loops, emit only on terminal poll exit, and `snapshot.inherit` on polling states is rejected. Evidence: `snapshot.inherit` with `poll` is rejected at `crates/rhei-validator/src/lib.rs:1130` and `crates/rhei-validator/src/lib.rs:1316`; gap: runtime poll emit behavior is absent because emission is no-op.

## Validation

- `VAL-007` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:928-929`): `snapshot.inherit.from: ancestor` on a root-task state is an error. Evidence: State-machine validation has no plan/task-root context; plan validation has no snapshot ancestor-root check. The only `from_axis` validation is value checking at `crates/rhei-validator/src/lib.rs:1090`.
- `VAL-012` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:938-949`): Static no-emitter and ambiguous-emitter references are errors, including nearest-ancestor ambiguity semantics. Evidence: No possible emitter and multiple same-machine emitters are rejected at `crates/rhei-validator/src/lib.rs:1196` and `crates/rhei-validator/src/lib.rs:1202`; gap: nearest-ancestor semantics cannot be validated because the state-machine validator lacks task hierarchy context.
- `VAL-013` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:950-954`): Static agent mismatch is an error when `required: true`; otherwise it emits a warning. Evidence: Required true with statically-known different agents is rejected at `crates/rhei-validator/src/lib.rs:1225`; gap: optional mismatch warning is not implemented.
- `VAL-014` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:955-962`): Named emit with a statically resolved agent profile lacking supported layout is an error; runtime-only resolution fails spawn; auto-emit has no validation diagnostic. Evidence: CLI settings validation rejects named emit without layout at `crates/rhei-cli/src/main.rs:6771`; gap: spawn-time failure and auto-emit runtime skip are missing because emission is no-op.
- `VAL-017` (`partial`, `docs/functional-spec/rhei-snapshots.spec.md:970-976`): Snapshot operations require a resolved effective target tuple; no effective tuple skips auto-emit and rejects explicit emit/inherit with `snapshot-requires-target`. Evidence: Explicit snapshot operations without invocations or slugs are rejected at `crates/rhei-cli/src/main.rs:6736` and `crates/rhei-cli/src/main.rs:6752`; gap: auto-emit runtime skip for no target is not implemented.
- `VAL-019` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:979-981`): `completion: timeout` snapshots are not valid authored inheritance sources; static checks reject known timeout-only sources and runtime checks reject selected timed-out generations. Evidence: No static timeout-only source model or runtime authored-inherit manifest check exists; preload is no-op at `crates/rhei-cli/src/main.rs:12463`.
- `VAL-021` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:985-986`): `snapshot.inherit` may not depend on a snapshot in a different plan workspace. Evidence: Authored inherit resolution is not implemented; the only workspace-boundary check is for CLI path refs under the configured cache at `crates/rhei-cli/src/main.rs:9340`, not state-machine inheritance.
- `VAL-022` (`missing`, `docs/functional-spec/rhei-snapshots.spec.md:988-992`): When the cache directory exists, validation compares manifests to the current plan/state machine and emits informational orphan warnings, never errors. Evidence: `validate_command` runs core and settings validations at `crates/rhei-cli/src/main.rs:3724` and `crates/rhei-cli/src/main.rs:3728` but does not scan `read_snapshot_records`; orphan detection exists only for snapshot commands at `crates/rhei-cli/src/main.rs:8965` and `crates/rhei-cli/src/main.rs:9840`.

## High-Level Themes

- Runtime snapshot emission is still absent: auto-emits, named emits, completion classification, atomic writes, manifest creation, current updates, target fanout, counted-loop visits, redaction, and Pi observed provider/model parsing all depend on the empty emission hook at `crates/rhei-cli/src/main.rs:12411`.
- Runtime snapshot inheritance is still absent: self/ancestor resolution, missing/incompatible/timeout fallbacks, native compatibility, cache-benefit logging, session staging, resume/fork flag injection, `parent_ref` tracking, and subtask branching all depend on the empty preload hook at `crates/rhei-cli/src/main.rs:12463`.
- Validation is mostly present for YAML grammar, but plan-aware and runtime-aware rules remain incomplete: root-task `from: ancestor`, nearest-ancestor ambiguity, optional cross-agent warnings, timeout-only source rejection, workspace-boundary inherit checks, and validation-time orphan warnings.

Recommended next state: `completeness-fix`.
