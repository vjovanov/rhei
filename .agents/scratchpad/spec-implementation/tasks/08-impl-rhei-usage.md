### Task impl-rhei-usage: Implement docs/functional-spec/rhei-usage.spec.md
**State:** pause
**Prior:** Task coordinate

Paused before first execution. Resume to `implement` when ready.

Implement the spec at `docs/functional-spec/rhei-usage.spec.md`. The full per-spec pipeline (completeness audit + quality review/fix loop) runs as part of this task. End-to-end coverage is handled by the shared `e2e-aggregate` task once every per-spec task is complete.
