[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rhei-api"
version = "0.4.2-dev"
description = "Python API helpers for Rhei markdown plans."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT OR Apache-2.0"
authors = [
  { name = "Rhei contributors" }
]
dependencies = [
  "rhei-cli==0.4.2-dev"
]
keywords = ["agents", "markdown", "planning", "workflow", "api"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Rust",
  "Topic :: Software Development"
]

[project.urls]
Homepage = "https://github.com/agent-grounds/rhei"
Repository = "https://github.com/agent-grounds/rhei"
Issues = "https://github.com/agent-grounds/rhei/issues"

[tool.setuptools.packages.find]
where = ["src"]
