# Soft (agent-facing) file-size exception registry (fissile spec FS-003-exceptions).
#
# Entries here silence soft-limit warnings for files this repo has deliberately
# accepted as oversized, each with a written reason and a maximum accepted size
# so the warning returns if the file grows past it. Add entries with
# `fissile exception add --severity soft` (fissile spec FS-005-exception-add).
#
# `kind` says which claim the entry makes, and therefore what `reason` has to
# establish (fissile spec FS-003-exceptions.2.1):
#
#   structural  splitting this file is illegal — name the constraint and what
#               would break. Nothing retires it, so `until` is "indefinite".
#   deferred    no constraint; a boundary is missing — name it and what has to
#               exist first. `until` names the condition that retires the entry.
#
# Neither is answered by describing what the file contains.

fissile_exceptions_version = 2

[[exceptions]]
path = "crates/rhei-cli/src/cli/run_agent_mode.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1000, unit = "lines" }
until = "Pass selection — collecting a pass's agent, program, and callback work and applying the non-concurrent claim filter — has a part of its own, leaving this file the run's setup, its pass driver, and its end-of-run report."
reason = """
The execution bodies are parts of their own now; what is left is the run's own lifecycle. Pass selection has no boundary to move to: it reads the plan, the machines, and the settings and writes the three task lists the pass driver dispatches, and those lists are locals of the loop. Setup and the end-of-run report stay beside it because the report is written from the counters the loop advances.
"""
[[exceptions]]
path = "examples/hourly-human-intervention-example/states.yaml"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 800, unit = "lines" }
until = "Split by template/example support if Rhei gains multi-file state machines."
reason = """
Example state machine intentionally shows a complete workflow in one file.
"""
[[exceptions]]
path = "crates/rhei-cli/templates/hourly-human-intervention/states.yaml"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 800, unit = "lines" }
until = "Split by template support if Rhei gains multi-file state machines."
reason = """
Template state machine mirrors the example as an instantiable workflow.
"""
[[exceptions]]
path = "crates/rhei-cli/templates/spec-implementation/states.yaml"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Split by template support if Rhei gains multi-file state machines."
reason = """
Template state machine must be copied as one instantiable workflow artifact.
"""
[[exceptions]]
path = "examples/spec-implementation-example/states.yaml"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "Split by template support if Rhei gains multi-file state machines."
reason = """
Example mirrors the spec-implementation template state machine.
"""
[[exceptions]]
path = "tests/e2e/completions_tests.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Split by shell or command group when new cases are added."
reason = """
E2E completion scenarios share setup and assertions.
"""
[[exceptions]]
path = "tests/e2e/templates_tests.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1300, unit = "lines" }
until = "Split by template command area when new cases are added."
reason = """
Template E2E scenarios share fixtures and setup.
"""
[[exceptions]]
path = "tests/e2e/next_tests.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1200, unit = "lines" }
until = "Split by readiness, assignee, and transition behavior when new cases are added."
reason = """
`next` command E2E scenarios share command fixtures.
"""
[[exceptions]]
path = "crates/rhei-cli/templates/changeset-review/states.yaml"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "Split by template support if Rhei gains multi-file state machines."
reason = """
Template state machine must be copied as one instantiable workflow artifact.
"""
[[exceptions]]
path = "tests/e2e/run_tests.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1400, unit = "lines" }
until = "Split by callback, agent, program, and snapshot behavior when new cases are added."
reason = """
Run-command E2E scenarios share setup and process assertions.
"""
[[exceptions]]
path = "crates/rhei-core/src/ast.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "Split workspace/task/state structs if more public fields are added."
reason = """
AST types are reviewed together as the core language model.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/tests_snapshot_runtime.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1900, unit = "lines" }
until = "Split into preload, emit, redactor, and fanout-target groups when new cases are added."
reason = """
Snapshot runtime tests share preload/emit/redactor fixtures and helper builders.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/snapshot_records.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 800, unit = "lines" }
until = "Split manifest IO from pi-header probing once a second native-header parser exists."
reason = """
Snapshot record loading, manifest schema, and pi-header parsing share serde types.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/tests_snapshots_gc.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 800, unit = "lines" }
until = "Split ref-gc from retention scans when retention policies grow."
reason = """
Snapshot GC tests share cache-root fixtures and generation builders.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/tests_settings_tooling.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1200, unit = "lines" }
until = "Split by load-vs-validate when settings gain new sections."
reason = """
Settings load/merge/validate tests share workspace and home-dir fixtures.
"""
[[exceptions]]
path = "tests/integration/integration_markdown_plans/run_programs_callbacks.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Split programs from callbacks when either grows a new behavior."
reason = """
Program-and-callback integration cases share workspace setup and process assertions.
"""
[[exceptions]]
path = "tests/integration/integration_markdown_plans/run_agent_regressions.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1100, unit = "lines" }
until = "Split by regression class (snapshot, fanout, polling) when new clusters appear."
reason = """
Run-agent regression cases share workspace setup and process assertions.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/tests_agent_execution_validation.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Split by validation surface (env, args, transport) when new surfaces are added."
reason = """
Agent execution validation tests share spawn-environment fixtures.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/snapshot_runtime_preload.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "Split override-contract checks from the preload pipeline if a second consumer needs them."
reason = """
Preload sequencing, override-contract validation, and parent_ref staging form one ordered procedure.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/tests_cli_render.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1300, unit = "lines" }
until = "Split by command group when new render surfaces are added."
reason = """
CLI render tests share golden output and command fixtures.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/system_transition_execution.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1100, unit = "lines" }
until = "Split per outcome class once routing policies diverge."
reason = """
System-transition execution carries the failure/timeout routing pipeline as one ordered procedure.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/tests_agent_resolution.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "Split by resolution surface (target, all_targets, legacy) when new surfaces are added."
reason = """
Agent resolution tests share execution-target fixtures.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/agent_command.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "Split subcommand handlers from dispatch when a fifth subcommand is added."
reason = """
`rhei agent` subcommand dispatch holds list/show/resolve/spawn next to each other for one help surface.
"""
[[exceptions]]
path = "tests/e2e/run_signals_tests.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1400, unit = "lines" }
until = "Split by shutdown class (signal, timeout, lost output) once the harness they share is a module of its own rather than helpers beside the cases."
reason = """
Signal, timeout, and lost-output cases share one process-supervision harness: spawning a real run, tracking its pid and grandchildren, and asserting on what survives each shutdown path.
"""
[[exceptions]]
path = "tests/integration/integration_markdown_plans/workspace_validation_panta.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1000, unit = "lines" }
until = "Split discovery from qualification and prior validation when panta gains a third cross-project surface."
reason = """
Cross-project panta cases share the multi-project fixture builders and the qualified-prior assertions that only apply to them.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/run_summary.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1700, unit = "lines" }
until = "The run report has a model type the Markdown writer and the TTY renderer both consume, so each renderer can move to its own module."
reason = """
Over budget before this change; the touch that surfaced it added six lines. The report model, its Markdown writer, and its TTY renderer are three surfaces behind one struct, and nothing separates the model from either renderer today.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/states_render.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1500, unit = "lines" }
until = "Plan loading moves to a module the render, scope, and validation paths all call, leaving this file the state-machine renderer."
reason = """
Over budget before this change; the touch that surfaced it added eight lines. Plan loading, scope reporting, and state-machine rendering share the file because plan loading has no home of its own.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/run_callback_mode.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 800, unit = "lines" }
until = "The pass scheduler shared with run_agent_mode.rs is factored out, leaving each mode its own execution body."
reason = """
Over budget before this change; the touch that surfaced it added four lines. It mirrors the agent-mode loop it was extracted alongside, and the two share a scheduler shape that is currently duplicated rather than factored.
"""
[[exceptions]]
path = "crates/rhei-cli/src/rhei_tui/tui/tests.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 810, unit = "lines" }
until = "The UiState fixture is shared from a test-support module so state, input, and view tests can live beside the code they cover."
reason = """
Over budget before this change; the touch that surfaced it added two lines, and the inspector-flags test for a person-waiting poll carried it 797 -> 807. One test module covers state application, key handling, and view derivation because they are exercised through one UiState fixture, and a view test cannot move out until that fixture does.
"""
[[exceptions]]
path = "crates/rhei-cli/src/rhei_tui/tui/state.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "View-derived projections take a narrowed borrow instead of &UiState, so they can move to the view modules that use them."
reason = """
Over budget before this change; the touch that surfaced it added six lines. UiState is one struct with one apply loop, and the view-derived helpers hanging off it cannot move without a borrow of the whole state.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/cli_declarations.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Subcommand groups move to their own Subcommand enums that the root flattens, so each group's declarations travel with its command."
reason = """
Over budget before this change; the touch that surfaced it added three subcommands. It is one clap derive tree, and a subcommand's declaration must sit inside the enum that clap parses.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/cli_dispatch.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Dispatch is split by the same subcommand groups the declaration tree grows, so each group's arms and its entry point travel together."
reason = """
Over budget before this change; the touch that surfaced it added three match arms. The dispatch match must name every subcommand in one place, and the process entry point and error rendering sit beside it.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/completion_candidates.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "Completers move next to the commands whose arguments they complete, once subcommand declarations are grouped into their own modules."
reason = """
Over budget before this change; the touch that surfaced it added one completer. Every completer is a free function clap references by name from the declaration tree, so they are collected rather than grouped.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/supervised.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 1000, unit = "lines" }
until = "The run's interruption state has a module the supervisor, the frontends, and the lock wait all call, so the process-group registry and the supervised-child lifecycle can move out from under it."
reason = """
Over budget before this change; the touch that surfaced it added an interruptible sleep to the shared helper. Run interruption state has no module of its own: the process-global stop token and the per-run shutdown flags sit beside the live process-group registry and the supervised-child lifecycle because those are the only readers, and nothing separates the state from the machinery that acts on it.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/completion_context.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 900, unit = "lines" }
until = "Plan-target and state-machine resolution move to a module the completion helpers, states, and list all call, leaving this file the completion context alone."
reason = """
Over budget before this change; the touch that surfaced it changed one completer's source. State-machine resolution has no module of its own — it shares this file with the shell-completion context helpers and with the states and list commands because all four answer 'which plan, under which machine' through the same free functions, and there is no resolution boundary for them to call instead.
"""
[[exceptions]]
path = "crates/rhei-cli/src/cli/snapshot_continue_lock.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 700, unit = "lines" }
until = "The run lock has a module of its own that run, the liveness probe, and the headless launcher call, leaving this file the snapshot-continue path alone."
reason = """
Over budget before this change; the touch that surfaced it deleted the blocking run-lock helper the interruptible wait replaced. The run lock has no module of its own: every run, liveness probe, and headless launcher takes or reads it, yet it lives beside snapshot-continue selection because that was the first caller, and there is no lock boundary for the others to call.
"""
[[exceptions]]
path = "crates/rhei-cli/src/rhei_validator/validator/state_defs.rs"
match = "exact"
rules = ["source"]
kind = "deferred"
max_accepted = { value = 600, unit = "lines" }
until = "the snapshot declaration structs move to a module of their own, leaving this file the state schema"
reason = """
Adding poll.waiting_on and its two accessors carried the file 491 -> 516. The split this file wants is not the poll schema, which belongs beside StateDef: it is the four snapshot declaration structs (StateSnapshotConfig, SnapshotEmitConfig, SnapshotInheritConfig and their serde contracts), a cohesive group with its own spec that reads as a second subject here. There is no module for them yet, and carving one inside a feature change would put an unrelated refactor in the same diff.
"""
